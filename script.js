/**
 * PassCraft Main Application Entrypoint
 * Bootstraps DOM initialization and state bindings.
 */

import { initDOM } from './src/dom.js';

document.addEventListener('DOMContentLoaded', () => {
  initDOM();
});